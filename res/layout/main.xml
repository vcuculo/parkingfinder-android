<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
     >

	<ImageView  android:id="@+id/logoImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo"
        android:layout_marginTop="20dip"
        android:layout_marginBottom="70dip"
	    />

    <Button
        android:id="@+id/searchButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/searchPark"
        android:layout_marginBottom="15dip"
      	android:text="@string/searchPark"
      	android:background="@drawable/buttonsearch"
      	android:textColor="@color/white"
      	android:textSize="24dip"
      	android:textStyle="bold"
      	android:padding="10dip"
      	android:minWidth="210dip"
      	android:alpha="0.5" />

   

    <Button
        android:id="@+id/releaseButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/releasePark"
        android:text="@string/releasePark"
        android:background="@drawable/buttonrelease"
      	android:textColor="@color/white"
      	android:textSize="24dip"
      	android:textStyle="bold"
      	android:padding="10dip"
      	android:minWidth="210dip"
      	android:alpha="0.5" />

</LinearLayout>